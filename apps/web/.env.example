# Base URL of the @utro/server API. Defaults to http://localhost:3000 if unset.
VITE_API_URL=http://localhost:3000

# Link opened by the "Присоединиться к курсу" button (external payment/landing page).
# Замените на прямую ссылку на оплату, когда она будет готова.
VITE_COURSE_URL=https://fajr-sestram.ru
